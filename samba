sudo apt update
sudo apt install samba -y
sudo mkdir -p /datos/nube
sudo nano /etc/samba/smb.conf

Meter esto en el archivo de smb.conf
[Nube]
   path = /datos/nube
   browseable = yes
   writable = yes
   read only = no
   guest ok = no
   valid users = tu_usuario
   force user = tu_usuario
   create mask = 0664
   directory mask = 0775

sudo smbpasswd -a tu_usuario
sudo smbpasswd -e tu_usuario
sudo chown -R tu_usuario:tu_usuario /datos/nube
sudo chmod -R 770 /datos/nube
testparm
sudo systemctl restart smbd nmbd
sudo systemctl enable smbd nmbd
